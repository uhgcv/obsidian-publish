    记录关键概念的名词，和关键理解。不用全部记录定�?- 课本的概念灌输本身太晦涩，不适合作为起步。不告诉你为什么要这个定义这个概念，为什么不要别的概念，它发明出来有什么目的，不知道。就像大街上突然冒出个人跟你倾诉了一堆东西，你为什么和我说这些？这些对我有什么用？不知道，不想听。还得是先建立关联再解释概念，先看科普视频后看书是对的�?
- 线性代数应该这样学：想要优先学这个。内容简练，思路清晰，排版养�?
### 张量
- 张量理解为维度化集合。张量维度数量称为阶数，阶数划分取决于应用需要，阶数的意义表示具有某种独有的内禀性质，可以理解为物理上的量纲。同阶的元素共有此量纲性质。坐标系中的标量、向量、矩阵，分别属于一种特殊定义的0阶�?阶�?阶张量�?    - 标量表示数值，没有多个元素并列也就�?维度，是1维数轴上�?维数值的抽象概念。向量是数值的�?维集合，所增张量维度就是数轴，数轴是基定义的。矩阵是向量的增一维集合，所增张量维度就是多个向量本身的集合�?        - 那么回到张量和矩阵等概念，可以推导出，坐标也只是量纲的一种，因此2阶张量和矩阵的关系，并非教科书的等同而是包含关系
- 也就是说坐标系和张量的维度概念容易同名理解混淆，坐标系维度可以一阶张量维度的集合。更高阶张量的意义可以理解为多个不同量纲一阶张量的整体的集合。比如二阶张量可以定义一阶是坐标本身、一阶是坐标点的集合，二者的集合这就是矩阵了�?- 应用中阶数的意义取决于需要的量纲。同一量纲的数据用一阶就没必要用多阶表示了。而且很抽象的是量纲可以有反身性定义，量纲可以定义为量纲的集合，像是表示集合本身就是一个量纲，而且可以多层嵌套下去，只要应用有必要�?

- [[组]]：是有序、可重复的集合。设计目的是组合多个域上的元素，是多维数据专用于统计的特化维度设计，向量是以组集合的标量，多个组能成为维度就是矩阵了。组可以由集合、函数方法构造。引入此概念为了表达直接�?- 向量：最广泛的含义是域的集合，有序性的目的是区分维度，区分维度需要额外定义，可以用符号表示，有序性是省略符号的一种简洁方法；可重复元素是域本身的统计性质，无需额外定义�?是一种有特殊形式规范的一阶张量，是无量纲、有序的、自集合的。维度可以是有限�?- 向量空间：就是规定了元素的代数规则，就是域。向量以意思就是想说明向量空间定义在域上，实数域Real或复数域Ccmplex，用Field概括域。角标表示定义的加群数量。以满足很多优良的代数性质�?- 子空间：无外乎参照子集的定义。直和的子空间之间交集为空集�?---

- 解的存在�?相容�?- 系数矩阵，增广矩阵，行变�?- 行化简，阶梯形矩阵，简化阶梯形矩阵，主元位�?
- 向量的定义：是一种一阶张量，由数轴上的数值取样组成元素的集合。所有元素为0的向量称为零向量
    - 向量的代数结构是域，R^n的向量有n列，本质是n个实数域的空间。因此满足域的运算规�?
这些概念的如何理解，课本介绍了很多概念近乎同一内涵的不同方面，为什么需要这些概念。这是我的感�?- 线性组合：是向量与标量的代数乘积，就是各向量元素标量的乘积，意义上是缩放，因此可以定义张成(若干向量所有空间，及其维度)，求向量的所有缩放空间在什哪一维度空间，等同于对此线性方程组的矩阵求解是否存�?    - 张成Span：是所有线性组合的集合
    - 矩阵方程：Ax=b。思路是用二阶张量的矩阵研究一阶张量的缩放，将缩放权重视为行矩阵x，用于缩放同元素数量的矩阵A中的向量（列）。定义此矩阵与向量的乘积的矩阵运算，等同于缩放代数运算。这一方法下，矩阵方程（矩阵的代数）、向量方程（向量的代数）、线性方程组（括号省略变量、行化简）统一了。解集是相同的。这些与几何意义表示的是同一实质的不同方面�?    - 线性变换：就是线性组合的几何理解。A就是变化后的基向量（变化前是各轴正方�?单位的基向量）的矩阵，x就是作为变化前的任意向量。运算结果的意义就是x通过A变换后的向量坐标。因此缩放本身就能推导出线性变换。用线性变换前后状态更直观理解矩阵方程的运算规�?    - 矩阵乘法：就是将矩阵乘以矩阵拆解为矩阵乘以向量的组合，就还原到了上面的原理。根本是继承了域的性质。向量之间组成矩阵的隐含规则是加法，矩阵方程是乘法，满足分配率，可拆分�?    - 线性方程组相比于矩阵，是行列翻转的，同维度是矩阵的行、而同变量是线性方程组的列

    - 平凡解：就是权重系数�? .因此变量乘以权重�?。对求变量没啥用�?    - 线性无关：一组向量乘以权重ax=0的等式，为了使其成立，所有系数的a=0（只有平凡解）�?        - 线性相关的设计目的是判断是否维度多余。但本身系数的解就是方程的解，系数a就是未知数本身，向量x是已知的不同方向的基向量。用于判断解的数量，线性无关只有唯一�?解，线性相关有无穷多解（在某个维度上，比如一条线或一个面）�?        - 向量�?*线性无�?*   ⟺  只有全�?的系�?⟺�?齐次方程�?Ax=0 **只有零解**。⟺
    - 线性相关：就是非线性无关。有非平凡解�?
| 现象 (行变换视�?      | 线性相关性视�?             | $Ax=b$ 的解的情�?   |
| --------------- | -------------------- | --------------- |
| 出现 $0 = 0$ 的全零行 | **线性相�?* (有自由变�?     | 如果有解，就�?*无穷多解** |
| 出现 $0 = 1$ 的矛盾行 | $b$ 不在列空间内 (这叫"不相�?) | **无解**          |
| 严格的上三角矩阵 (无全零行) | **线性无�?* (满秩)        | 如果有解，就�?*唯一�?*  |


- 向量乘法是一种定义运算，称为线性变换，作用是空间映�?- 选取基向量能确定张成，通过每个基缩放后加和。基向量的线性变换因此可以推广到整个空间。线性变换矩阵因此是基的缩放标量构成的向量�?
- 定义思维：衍生运算是定义出来的。理解概念有效方法是，拆解到基本公理，往往看得更清楚。在线性代数，运算总要拆解到一维的数轴，即满足域的运算，这样才方便元素加法和乘法运算�?- 内积：运算目的是两个向量的一维上的乘积。转换为一个维度要用到投影，因此需要夹角余弦、模长�?- 外积：运算定义为元素分别相乘�?

- �?
- 张成


- 线性变换：
    - 最妙的是分解法。二维变换前的任何向量都能分解为两个基的缩放scaling。变换后的基在变换前的空间坐标，即可作为特征值，推广计算变换后的所有张�?    - 矩阵就是若干并列向量而已�?    - 可以推论：向量乘矩阵的运算方法，相乘的本质是基的缩放scaling。而基的缩放本质上是一维数轴上的乘法�?    - 可以推论：矩�?乘矩�?的运算方法，即矩�?所含向量都经过矩阵1线性变换。这是多层运算的嵌套，哈哈�?        - 推论：矩阵从右向左存在运算顺序，不满足交换，即改变位置。满足结合，因为先算哪个都不改变位置�?
- 行列式：det。表示缩放后与缩放前的面积比例。负数表示翻转。可以从基计算�?- 通用计算公式：对于基(a,b)(c,d)，基是ad-bc。可视为两个矩形公式相减，ad管正调节，bc管负条件�?    - 自我推导：若基选择x轴上任意向量I，任意向量的J，原面积=Ix �?Jy 

- [[点积]]

[[解的理解]]

[[矩阵的代数结构]]