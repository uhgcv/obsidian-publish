
https://docs.godotengine.org/
按照官网文档逐步学习�?

- **符号含义**
- ：：表示从属开�?- TAB：表示从�?- 空格：表示分�?- _:表示文本分隔

- 教程很重视预期管理。制作组的诚意满满，令人安心

- library：库�?- function：函数�?- argument：命令�?- parameter：参数。无参数调用，以数学上的函数理解可视为参数为0。自定义参数可作为原参数的占位符

- functions
    - （func 〖name�?)：）：函数自定义，〖name〗替换为自定义文本，name文本需要相互独立、不能用空格、只能下划线或字母开�?    - return：返回此函数的值，给调用此函数的代�?其他函数调用直接写名称，不用func


- variables：变�?    - **本质**：变量是向量。副变量是每个坐标轴上的值。变量是数组的列。array是本质是数组，所以元素可以是值或向量�?- var 变量�?= 数值：定义变量
    - 变量数值计算符号是等号=，而非等于的意思。数学上的等于是== ，不等于是！= 。单用是赋值，以最新一次赋值为�?    - （变�?= 变量+变化值）可简写为（变�?+= 变化值），减法同�?    - var 变量�?: 值类�?= 数值：指定变量的值的类型。省略值类型可让电脑自动检测适配类型�?- sub-variables：副变量。即一个变量函数的从属变量，一个多变量函数的其中一个变量名。特别地要加个点表示，变量名. 〖sub-variables�?= 数值�?

- 数值类型：
    - Vector2
    - String
    - Int
    - Float

- _process：Godot 引擎每帧自动调用的函数（类似"心跳"�?- delta：每帧对应的现实时间�?    - 帧数x每帧时间=现实时间
~~~
func _process(delta):
当中，每帧时间都执行，相当于频率，数值需要再乘一个帧时间，才等于显示时间
~~~
- y轴正方向向下，x轴正方向向右，旋转正方向是顺时针

（变�?= 变量+变化值）

~~~
func _process():
###循环函数。当赋予任意值时触发，否则不触发
set_process(true)可以触发
~~~

- while+条件+�?TAB。按照条件循环执�?- for+条件+�?TAB。按照数值循环执行次数。默认包含自定义变量
    - for number in range (X) ：输入一遍所有X-1的自然数
    - 〖element�?in [0, 1, ..., X-1, X]：输入一遍所有括号内的数值，〖element〗可以是一个自定义变量，不用额外定义因为for默认包含了。本质上是自定义一个数列包含一组数值，再定义另一个变量，数列从前向后顺序，用变量=数列中的特定值�?
- array：本质是数组，列可包含任意值（，数值或参数可以是多变量），形式�?[�? 值] ，注意逗号后空格�?    - 数组名称+.append()：添加元素到数组
    - 数组名称[]：值是自然�?�?开�?，以调用第正整数行。正数是正顺序，负数是负顺序(�?1开�?
    - size是array自带的子函数，统计数组行�?- dictionary：本质是映射，是两个数组的列关联命名关联的集合，前者名为key，后者名为value。列可包含任意值，形式�?{�? 值}
    - 调用前者等于调用后者关联列，因为前者string本质是个array，所以调用语法是dictionary["string�?]，输出的是value。调用value需要先调用key
- range(X)：所有X-1的自然数


- time frame是一个游戏可设置的时间单位，最小单位为delta

    - if/else条件后面要加：和tab表明从属

- Vector2：二维向量函数，有两个副变量，Vector3是三维向量函数有三个副变�?- position：位置函数，根据2D�?D。有两或三个副变�?
- lerp：取比例，终值，初始值，比例。输出的�?终�?初始�?x比例
    - 初始值是个变量才有意义，否则不如直接用比例。意义是随时间平滑变化。初始值若能按时间取，则变化速率不断下降趋近终值。终值也可以是变量，则可呈现缓急交替的阻尼感�?    - 这个函数好棒啊，可以模拟软弹的肉�